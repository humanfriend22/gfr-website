rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow write: if false;
      allow read: if true;
    }
    match /users/{documentId} {
      allow read, update: if request.auth != null && request.auth.uid == documentId;
    }
    match /seasons/{seasonId} {
        allow read: if true;
        match /teams/{teamId} {
            allow read: if true;
            allow update: if 
        }
    }
  }
}